<mvc:View
    controllerName="nmshd.app.flows.account.relationship.Master"
    xmlns:m="sap.m"
    xmlns:layout="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:semantic="sap.m.semantic"
    xmlns:html="http://www.w3.org/1999/xhtml"
    xmlns:form="sap.ui.layout.form"
    xmlns:controls="nmshd.app.core.controls"
>

	<semantic:DetailPage
        id="page"
        navButtonPress="onNavButtonPress"
        showNavButton="true"
        class="{v>/theme/fontStyle}"
        title="{v>/title}"
        showFooter="false"
        busy="{v>/busy}"
        busyIndicatorDelay="{v>/delay}"
    >


		<semantic:customHeaderContent>

		</semantic:customHeaderContent>

		<semantic:subHeader>
		</semantic:subHeader>

		<semantic:content>

			<m:List items="{v>/navigation}" itemPress="onItemPress">
				<m:StandardListItem title="{v>title}" icon="{v>icon}" type="Navigation" />
			</m:List>
		</semantic:content>
	</semantic:DetailPage>
</mvc:View>
